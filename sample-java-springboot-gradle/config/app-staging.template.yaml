apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-staging-deployment
  labels:
    app: my-app-staging
  namespace: default
spec:
  replicas: 1 # from-param: ${replicaCount}
  selector:
    matchLabels:
      app: my-app-staging
  template:
    metadata:
      labels:
        app: my-app-staging
    spec:
      containers:
      - name: nginx
        image: my-app-image
        ports:
        - containerPort: 8080